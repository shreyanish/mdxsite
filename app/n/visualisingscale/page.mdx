import { AnimatedName } from '../animated-name.tsx';

export const metadata = {
  title: 'Visualising Scale',
  alternates: {
    canonical: '/n/dx'
  }
};

export function Title({children}) {
  return (
    <span className="font-serif text-2xl">{children}</span>
  )
}

export function Tldr({ children }) {
  return (
    <span className="text-sky-400">TL;DR <span className="text-white">{children}</span></span>
  );
}

<Title>Visualising Scale</Title>

<AnimatedName />

<Tldr>Understanding how earth went from no money to so much money and how to direct the flow from its people to you.</Tldr>